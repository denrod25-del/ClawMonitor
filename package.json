{
  "name": "clawmonitor",
  "version": "0.1.0",
  "description": "Always-on-top neon system monitor bar for Windows",
  "main": "main/index.js",
  "scripts": {
    "start": "electron .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "systeminformation": "^5.22.0"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "vitest": "^2.0.0"
  }
}
